## Things to improve based on feedback
- More UI information: dont close this window
- Focus attention on `Create Room` button. Remove paste code from the home page
- Focus attention on `Share this link`
- Folder/file selection is unclear. Prioritize file first, or do something else about it
- Interface is confusing. Add file display?
- Not clear why files dissapear on page reload. Prevent page reload when files were selected

## Technical tasks
- Make all failures non-fatal

## Unhandled/unexpected errors
 - Failed to addIceCandidate
 - `Unexpected error: failed to fetch` when signaling server is down
